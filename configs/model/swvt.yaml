# @package model

# SWVT (Video Swin Transformer) 模型配置
# 基于 3D Shifted Window Attention 的时空联合建模架构

name: "SWVT"

params:
  in_channels: 1
  out_channels: 1
  hidden_dim: 96
  num_layers: 4
  num_heads: 4
  window_size: [2, 8, 8]
  dropout: 0.0
  use_checkpoint: true
