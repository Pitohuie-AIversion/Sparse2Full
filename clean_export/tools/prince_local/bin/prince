#! /bin/sh

SCRIPT_DIR=$(cd "$(dirname "$0")" && pwd)
PRINCE_LIB=$(cd "$SCRIPT_DIR/../lib/prince" && pwd)

if [ -f "$PRINCE_LIB/bin/prince" ]; then
    exec "$PRINCE_LIB/bin/prince" --prefix="$PRINCE_LIB" "$@"
else
    exec prince "$@"
fi
