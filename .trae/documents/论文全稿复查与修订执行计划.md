# 论文全面复查与修订计划

我已完成对论文全稿（Chapter 0-9）及核心代码的深度比对阅读。根据您的要求，我制定了如下三阶段修订计划，旨在从内容逻辑、学术规范到数据一致性进行全方位提升。

## 第一阶段：逻辑架构与内容一致性修复
**目标**：消除“方法—实现—实验”之间的逻辑断裂，确保代码中的最新改动（如 AR 损失、三阶段训练）准确反映在论文中。

1.  **Chapter 3 (Methodology) 同步**：
    *   **修正**：更新“序列化训练”部分的描述，使其与 `sequential_trainer.py` 中的 `_train_joint_epoch` 实现完全对应（明确 Spatial -> Temporal -> Joint 的具体冻结策略）。
    *   **修正**：明确“低频谱损失”的定义为 Low-pass Filter（与 `ops/losses.py` 中的 `k_max` 截断逻辑一致），而非带通或其他形式。
2.  **Chapter 5 (Implementation) 增强**：
    *   **补充**：在伪代码或实现细节中，补充 `compute_ar_total_loss` 中的“导数一致性”与“能量演化一致性”项，确保第 6 章的 AR 实验有实现支撑。
    *   **核对**：检查超参数（如 `k_max=16`）在文中声明与代码默认值的一致性。
3.  **Chapter 4 (Theory) 连贯性**：
    *   **调整**：确保理论命题（如“评测一致性上界”）中的符号定义（$H, DC, \tilde{u}$）与 Chapter 0 符号表严格统一。

## 第二阶段：学术规范与格式标准化
**目标**：统一全书的引用风格、数学符号与排版格式，达到研究生学位论文的规范要求。

1.  **引用格式统一（APA 风格）**：
    *   **现状**：目前文中混用 `[1]` 与 `(Author, Year)`。
    *   **行动**：全书统一采用 **(Author, Year)** 格式（如 `(Kovachki et al., 2023)`），以匹配 Chapter 8/9 中的参考文献列表格式，避免手动维护数字编号的错乱风险。
2.  **数学符号规范化**：
    *   **强制统一**：
        *   评测口径误差：统一为 $H_{\mathrm{err}}$（正体下标）。
        *   退化算子：统一为 $\mathrm{DC}$（正体）。
        *   向量/矩阵：统一使用加粗（如 $\mathbf{u}, \mathbf{x}$）或统一不加粗（需确认风格），建议全书统一为非加粗斜体以符合物理类论文习惯，或仅对坐标 $\mathbf{x}$ 加粗。我将采用**物理量斜体 ($u$)、坐标/算子正体或花体 ($\mathbf{x}, \mathcal{F}$)** 的标准范式。
3.  **Markdown 排版优化**：
    *   统一公式块标记（统一使用 `$$` 或 `\[`）。
    *   统一三级标题的层级结构。

## 第三阶段：语言润色与查重规避
**目标**：提升学术表达的专业性，并修正潜在的语病。

1.  **术语统一**：
    *   锁定关键词：`Swin-UNet` (统一带连字符), `Sequential Training`, `Consistency-First`。
2.  **表达修正**：
    *   扫描并修改口语化表达（如“我们发现”、“效果很好”改为“实验结果表明”、“性能显著提升”）。
3.  **原创性说明**：
    *   *注：我无法访问外部查重数据库，但我将通过规范化引用（明确标注 Reference）和重写通用描述来降低潜在的查重率。*

## 交付物
1.  **修订后的全套 Markdown 源码**（Chapter 0-9）。
2.  **修改建议清单（Revision Report）**：详细列出每个章节的具体修改点、修改理由及对应页码/段落。

请确认是否启动上述修订计划。