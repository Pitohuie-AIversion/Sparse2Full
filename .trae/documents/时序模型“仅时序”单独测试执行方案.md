## 目标
- 使用 CPU 运行“仅时序预测”训练与验证，避免占用 GPU。
- 统一批量与线程，保证在 CPU 上稳定可跑、速度可接受。

## 需修改的配置（目标文件：`/share/fandixiaLab/suguangsheng/PycharmProjects/Sparse2Full/thesis_paper/configs/temporal/ar_training_config_debug_temporal_cpu copy.yaml`）
- 设备
  - `experiment.device: cpu`
  - `device.accelerator: cpu`
  - `device.devices: 1`
  - `device.strategy: none`
  - `device.precision: fp32`
  - `training.amp.enabled: false`（保留）
  - `training.distributed.enabled: false`；`training.distributed.backend: gloo`
- 批量与加载器（统一为 CPU 友好设置）
  - `data.dataloader.batch_size: 8`
  - `data.dataloader.val_batch_size: 8`
  - `data.dataloader.test_batch_size: 8`
  - `data.dataloader.num_workers: 0`（CPU 调试更稳；若数据 IO 不重可改 2）
  - `training.batch_size: 8`
  - `training.dataloader.batch_size: 8`
  - 将被误放在 `training.loss_weights` 下的 DataLoader 键（`val_batch_size/test_batch_size/num_workers/pin_memory/persistent_workers/prefetch_factor/drop_last/shuffle`）移回各自块或删除，以免失效
- 日志（避免 GPU 字段无意义）
  - `logging.performance_monitoring.log_gpu_memory: false`
- 线程（CPU 性能与可复现实验兼顾）
  - `hardware.torch_threads: 8`
  - `hardware.mkl_threads: 8`
  - `hardware.omp_threads: 8`
  - `hardware.interop_threads: 8`
  - `hardware.blas_threads: 8`
  - 保留 `allow_tf32: false`，`vram_threshold` 可忽略（在 CPU 无效）
- 其他保持不变
  - `sequential.enabled: true` 与 `model_wrapper.enabled: false`
  - `sequential.training.two_stage: false`（只训练时序路径；参见 tools/training/train_real_data_ar.py:3249–3257）
  - `data.observation.mode: identity`（禁 DC）
  - 课程：`training.curriculum.stages: [{T_out:1,25},{T_out:3,35},{T_out:5,40}]`；`teacher_forcing_decay: 0.95`

## 运行方式
- 训练：
  - `python tools/training/train_real_data_ar.py --config "/share/fandixiaLab/suguangsheng/PycharmProjects/Sparse2Full/thesis_paper/configs/temporal/ar_training_config_debug_temporal_cpu copy.yaml"`
- 训练脚本要点：
  - 自动识别分阶段模型并启用（tools/training/train_real_data_ar.py:1582）。
  - 非两阶段模式下直接按序列损失训练（tools/training/train_real_data_ar.py:3400–3565）。

## 验证
- 对比 step-by-step vs one-shot：
  - `python tools/validation/validate_temporal_no_sr.py --config "/share/fandixiaLab/suguangsheng/PycharmProjects/Sparse2Full/thesis_paper/configs/temporal/ar_training_config_debug_temporal_cpu copy.yaml"`
  - 调用 `rollout_inference` 两种路径（models/temporal/components/sequential_spatiotemporal.py:655–694；验证脚本 tools/validation/validate_temporal_no_sr.py:39–46）。

## 风险与调整
- 若 CPU 慢或 OOM：将批量降到 `4`，`num_workers` 继续保持 `0`。
- 若仍有 GPU 相关错误：确认分布式后端为 `gloo` 且 `AMP` 关闭；日志禁用 GPU 内存记录。

## 交付
- 我将按上述项更新 YAML 并返回修改后的文件；随后可直接用上述命令在 CPU 上运行。

## 参考
- 分阶段模型识别与训练：tools/training/train_real_data_ar.py:1582、3249–3257、3400–3565。
- 时序 rollout 与验证：models/temporal/components/sequential_spatiotemporal.py:571、655–694；tools/validation/validate_temporal_no_sr.py:39–46。