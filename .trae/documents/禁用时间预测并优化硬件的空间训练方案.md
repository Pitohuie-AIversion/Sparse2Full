## 目标
- 禁用时间预测，仅进行空间预测（单帧输入/输出）。
- 依据开发文档硬件建议优化 CUDA/AMP/DDP 与 DataLoader 并行。
- 使用当前训练入口 `\share\fandixiaLab\suguangsheng\PycharmProjects\Sparse2Full\tools\training\train_real_data_ar.py` 与配置 `\share\fandixiaLab\suguangsheng\PycharmProjects\Sparse2Full\configs\train\ar_training_config debug.yaml`。

## 配置修改（空间-only）
- 推荐直接改用 `configs/train/ar_training_config_spatial_only.yaml`，其已为纯空间模式：
  - `sequential.enabled: false`（`configs/train/ar_training_config_spatial_only.yaml:157-159`）
  - `ar.enabled: false`（`configs/train/ar_training_config_spatial_only.yaml:159-163`）
  - `data.T_in: 1`、`data.T_out: 1`（`configs/train/ar_training_config_spatial_only.yaml:22-24`）
  - 单帧模型：`model.name: SwinUNet`（`configs/train/ar_training_config_spatial_only.yaml:103-118`）
- 若必须沿用 `ar_training_config debug.yaml`，请对齐以下关键项：
  - 确认 `ar.enabled: false`（`configs/train/ar_training_config debug.yaml:19-22`）
  - 设为单帧：`data.T_in: 1`、`data.T_out: 1`（`configs/train/ar_training_config debug.yaml:34-36`）
  - 添加顶层 `sequential.enabled: false`（与 spatial_only 保持一致）
  - 选择单帧模型：`model.name: SwinUNet`（参考 spatial_only 模型段），避免 `ARWrapper`
  - 保留或移除 `ar_config.*` 无影响（在 `ar.enabled=false` 下不生效）

示例片段（供调试版配置对齐）：
```
# 顶层
sequential:
  enabled: false
ar:
  enabled: false

data:
  T_in: 1
  T_out: 1

model:
  name: SwinUNet
```

## 训练脚本约束
- 仅空间阶段：确认或保持 `training_phase = 'spatial'`（`tools/training/train_real_data_ar.py:178-179`）。
- `get_current_T_out(epoch)` 在纯空间下应恒为 1（`tools/training/train_real_data_ar.py:188-208`）。
- 初始化路径：时空模型/训练器的创建段（`tools/training/train_real_data_ar.py:1901-1954`）在 `sequential.enabled=false` 时只走空间分支。

## 数据管线与形状
- 数据模块按 `T_in/T_out` 生成序列：`__getitem__`（`datasets/real_diffusion_reaction_dataset.py:339-394;375-389`）。
- 设置 `T_in=1`、`T_out=1` 后，张量形状应为 `[1, C, H, W]`；构建 `DataModule` 时读取到单步（`datasets/real_diffusion_reaction_dataset.py:425-427`）。
- DataLoader 参数位置：`train/val/test_dataloader`（`datasets/real_diffusion_reaction_dataset.py:743-805`）。

## 硬件优化设置
- 依据开发文档（`docs/DEVELOPMENT.md:206-240`；并参考调优建议 `docs/DEVELOPMENT.md:274-306`）：
  - 设备并行：`device.accelerator: cuda`（`configs/train/ar_training_config debug.yaml:13-18`），`devices: <GPU数量>`，`strategy: ddp`。
  - 精度：优先 `training.precision: bf16-mixed`（支持 BF16 的 GPU），否则用 `fp16-mixed`（`configs/train/ar_training_config debug.yaml:220`）。
  - 允许 TF32 与 cuDNN benchmark：`hardware.allow_tf32: true`；脚本中启用 `cudnn.benchmark`（`tools/training/train_real_data_ar.py:681-706;793-805;854-886`）。
  - DataLoader 并行：`data.dataloader.num_workers: 32`、`prefetch_factor: 16`、`persistent_workers: true`、`pin_memory: true`（`configs/train/ar_training_config debug.yaml:68-81`）。
  - 性能监控与自适配：开启 `performance_monitoring.enabled: true` 并设置阈值（`configs/train/ar_training_config debug.yaml:146-156`）。
  - DDP/NCCL 稳定性：脚本已处理 `MASTER_ADDR/PORT` 与 `NCCL_*`（`tools/training/train_real_data_ar.py:611-651;708-748`）。

## 运行与验证
- 运行入口保持不变，使用更新后的空间-only配置：
  - 示例：`python tools/training/train_real_data_ar.py --config configs/train/ar_training_config_spatial_only.yaml`
- 验证点：
  - 日志中的 `input_sequence/target_sequence` 形状为 `[1, C, H, W]`（`tools/training/train_real_data_ar.py:1348-1350`）。
  - 时间相关指标退化为单步或被忽略，损失以空间重建/一致性为主。

## 需要你确认
- 是否改用现成的 `ar_training_config_spatial_only.yaml`，或在 `ar_training_config debug.yaml` 中套用上述最小变更。
- GPU 数量与是否支持 BF16，以确定 `devices` 与 `training.precision` 的最终取值。