# 论文与代码一致性审查及修复计划

经过对论文（Chapter 0-6）与核心代码（`train_real_data_ar.py`, `sequential_trainer.py`, `losses.py`）的详细比对，我发现了以下关键的一致性问题，并制定了修复计划。

## 1. 核心一致性断裂：序列化训练策略 (Critical Gap)
- **论文陈述**：Chapter 3 & 5 明确提出“三阶段训练策略”：
  1. 空间预训练 (Spatial Pretraining)
  2. 时序预训练 (Temporal Pretraining)
  3. **联合微调 (Joint Fine-tuning)**：解冻所有参数，进行 AR 滚动预测与时序正则化。
- **代码现状**：
  - `models/temporal/components/sequential_trainer.py` 中 `_train_joint_epoch` 方法为空（标注为 `# TODO: 实现联合训练逻辑`）。
  - `train_real_data_ar.py` 依赖该 Trainer，导致实际运行时“第三阶段”缺失或无效。
- **修复方案**：
  - 在 `sequential_trainer.py` 中完整实现 `_train_joint_epoch`，包含：
    - 联合优化的 Loss 计算（Spatial + Temporal）。
    - 引入 Teacher Forcing Decay 策略。
    - 启用 AR 滚动预测（Rollout）与时序一致性损失（`derivative/energy consistency`）。

## 2. 论文内容修订与增强
- **Chapter 6 (实验设置)**：
  - 当前内容较为通用（"采用 PDEBench"）。
  - **行动**：根据代码引用（`RealDiffusionReactionDataModule`, `DarcyFlowDataModule`），在 6.1 节明确补充具体使用的 PDE 子集（Diffusion-Reaction, Darcy Flow）及分辨率配置，消除模糊性。
- **符号与术语统一**：
  - 确保全文 $H_{err}$ 定义统一为原值域误差 $\|H(\tilde{u}) - y\|_2$。
  - 检查并移除 Markdown 文件中残留的非正式注释或 TODO 标记。

## 3. 代码逻辑对齐
- 验证 `ops/losses.py` 中的 `compute_ar_total_loss` 是否被正确调用，并确保其权重配置（`w_rec`, `w_spec`, `w_dc`, `w_deriv`, `w_energy`）与论文 Chapter 3.5 描述一致。

## 执行步骤
1. **代码修复**：实现 `sequential_trainer.py` 中的联合微调逻辑。
2. **论文修订**：更新 Chapter 6 的实验设置细节，使其与代码中的 Dataset 对应。
3. **最终核验**：再次检查论文中关于“三元损失”与“训练阶段”的描述是否与修复后的代码完全吻合。