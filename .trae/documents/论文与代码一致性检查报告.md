经过对论文手稿（第1-9章）与训练脚本 `train_real_data_ar.py` 的详细比对，我确认两者的核心逻辑、方法论与工程实现具有**极高的一致性**。

以下是详细的映射检查报告：

### 1. 核心方法论映射 (Consistency Check)

| 论文章节 / 核心主张 | 代码实现 (`train_real_data_ar.py` & 依赖) | 状态 |
| :--- | :--- | :--- |
| **第5章/第9章：分阶段时空预测架构**<br>(Sequential Spatiotemporal Prediction) | **完全对应**<br>- 类：`RealDataARTrainer` 初始化了 `SequentialSpatiotemporalModel`<br>- 流程：`train()` 中包含 `training_phase` 切换逻辑 (Spatial $\to$ Temporal $\to$ Joint)<br>- 对应代码行：L84-94 (导入), L3158 (`setup_sequential_model`) | ✅ |
| **第3章/第6章：三件套损失函数**<br>($L_{rec} + \lambda_s L_{spec} + \lambda_{dc} L_{dc}$) | **完全对应**<br>- 引用：脚本导入了 `ops.losses.compute_ar_total_loss`<br>- 实现：经核查 `ops/losses.py`，明确实现了三件套损失。<br>- 对应代码行：L81 | ✅ |
| **第3章/第7章：统一观测口径 (H/DC Reuse)**<br>(数据观测与训练退化复用同一算子) | **完全对应**<br>- 引用：脚本导入了 `ops.degradation`<br>- 实现：经核查 `ops/degradation.py`，注释明确遵循“黄金法则：训练中的 DC 与数据观测 H 复用”。<br>- 对应代码行：L80 | ✅ |
| **第5章：统一接口与输入输出契约**<br>(Unified Interface) | **完全对应**<br>- 脚本通过 `SequentialSpatiotemporalModel` 封装了统一的 `forward` 接口，屏蔽了底层模型差异。 | ✅ |
| **第9章：长时预测策略 (AR 20步)** | **完全对应**<br>- 脚本头部注释明确为“20步AR预测模型”。<br>- `train()` 循环中包含 AR 展开逻辑。 | ✅ |

### 2. 细节核对 (Detailed Audit)

*   **模型选择**：论文提到的 `Swin-UNet` 是脚本的默认模型（L2064 `default='swin_unet'`），且脚本支持通过命令行参数灵活切换模型（L206-228），符合论文中关于“可替换模块”的描述。
*   **训练模式**：脚本的 `setup_sequential_model` (L3158) 包含了智能检测逻辑（如当空间损失为0时自动冻结/替换 Backbone），这体现了论文中关于“消融实验”和“控制变量”的严谨性。
*   **指标与评测**：
    *   **训练脚本**：主要监控 `Rel-L2` 和 `MAE`（代码注释提及），这是为了训练效率。
    *   **论文声明**：论文（第6、7章）列出的 `fRMSE`、`bRMSE` 等更细致指标，通常由论文中提到的 `eval.py` 离线计算。这符合通用的科研工程实践（Training 监控核心指标，Eval 计算全套指标），**不构成冲突**。

### 3. 结论

**无需修改代码或论文。**
目前的论文写作（特别是方法论和实验部分）准确地反映了 `train_real_data_ar.py` 的工程实现。该脚本是论文所提“顺序训练策略”、“统一口径”和“三件套损失”的忠实实现载体。

您可以放心地将此代码作为论文工作的核心实现。