# 计划：添加观测口径错配影响的对比图

## 1. 目标与总结
根据用户的建议，在《第四章 结果验证》中补充一张柱状图，直观展示“观测口径错配对观测一致性误差 ($H_{\mathrm{err}}$) 的破坏”，从而增强论文的论证说服力。新图将插入到现有“表 4-6 口径错配影响分析”的结论之后。

## 2. 当前状态分析
- 错配实验数据位于 `thesis_paper/manuscript_5_chapter/chapter4_results_verification.md` 的“表 4-6”中。
- 当前文档仅使用表格展示了 Consistent、Mismatch（轻微）、Mismatch（严重）的数值变化，缺乏直观的视觉对比，读者容易忽略 $H_{\mathrm{err}}$ 显著上升而 Rel-L2 基本不变的关键现象。
- 当前章节的图号排到了图 4-10，在表 4-6 处插入新图后，后续图号需要顺延。

## 3. 具体修改方案

### 3.1 编写绘图脚本
- **文件路径**：`tools/plot_fig4_8_mismatch.py`
- **实现细节**：
  - 使用 `matplotlib` 绘制柱状图，X轴为三种错配程度（Consistent, Mismatch (轻微), Mismatch (严重)），Y轴为 $H_{\mathrm{err}}$。
  - 数据提取自表 4-6（分别为 0.0056, 0.0073, 0.0107）。
  - 在柱子上标注具体数值。
  - 样式对齐项目中已有的论文配图风格（`seaborn-v0_8-paper`, Times New Roman 字体）。
  - 输出图片至：`thesis_paper/manuscript_5_chapter/images/fig4-8_mismatch_herr.png`。

### 3.2 更新 Markdown 文档
- **文件路径**：`thesis_paper/manuscript_5_chapter/chapter4_results_verification.md`
- **修改内容**：
  1. 在第 219 行（“...产生了系统性的偏差。”）之后，插入用户提供的正文说明与图例：
     > 图 4-8 进一步表明，观测口径错配对观测一致性误差的影响远强于其对全场重建指标的影响。为更直观展示观测口径错配的影响，图 4-8 给出了不同设置下 $H_{\mathrm{err}}$ 的变化情况。可以看出，随着训练退化算子与测试观测算子之间差异增大，$H_{\mathrm{err}}$ 明显上升，而 Rel-L2、PSNR 与 SSIM 的变化相对有限。这说明观测一致性误差能够更敏感地反映模型在观测子空间中的系统偏差。
  2. 插入新图：
     > `![图 4-8: 不同观测口径设置下的观测一致性误差对比。随着错配程度的加剧，$H_{\mathrm{err}}$ 显著恶化，而常规重建指标变化较小。](images/fig4-8_mismatch_herr.png)`
  3. **调整后续图号**：
     - 原 `图 4-8: 损失函数消融...` -> 改为 `图 4-9`
     - 原 `图 4-9: 序列化课程...` -> 改为 `图 4-10`
     - 原 `图 4-10: 不同模型架构...` -> 改为 `图 4-11`

## 4. 假设与决策
- **决策**：用户留言中提到“表 4.7 已经说明了数值变化...”，但查阅源码发现“口径错配”的表格实为 **表 4-6**，表 4-7 是损失函数消融。此处以实际的 **表 4-6** 数据为准进行绘图。
- **决策**：采纳用户的“最推荐画法A”，即仅绘制 $H_{\mathrm{err}}$ 以突出结论。可选的“重投影误差示意图”因需使用外部工具（如 Drawio/PPT）手动绘制原理图，故暂不包含在代码生成的计划内。

## 5. 验证步骤
1. 执行 `python tools/plot_fig4_8_mismatch.py` 确保无报错且图片生成正确。
2. 检查 `chapter4_results_verification.md` 中文本承接是否自然，图号是否完全连续（4-1 至 4-11 无冲突）。