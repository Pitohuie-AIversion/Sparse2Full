I will fix the issues in the thesis manuscript following your P0 -> P1 -> P2 priority list.

### Plan of Action

#### 1. P0: Hard Consistency & Formatting Fixes (Priority: High)
*   **Fix Formula Syntax**: In `chapter4_results_verification.md`, correct `MSE(H(u),, DC(u))` to `MSE(H(u), DC(u))`.
*   **Fix Formula Numbering**: In `chapter4_results_verification.md`, replace all `(7-x)` numbering with standard `(4-x)` format to match the chapter number.
*   **Fix Table Numbering & Organization**:
    *   Ensure all tables in Chapter 4 have unique, sequential numbering (Table 4-1 to 4-10).
    *   **Table 4-8/4-9 (Resource Table)**: Split the resource comparison table into two distinct sections or tables: one for "Spatial Reconstruction (SR)" and one for "Spatiotemporal Modeling (AR-DR2D)" to avoid unfair comparison.
*   **Align Propositions**: In `chapter2_problem_framework.md`, add **Proposition 2.3 (Robustness against Aliasing/Grid Shift)** to align with the "Three Propositions" claim in Chapter 4.12.
*   **Fix References**: In `chapter5_discussion_conclusion.md`, remove the LaTeX-breaking `$...$` wrappers around links and remove `utm_source` tracking parameters.

#### 2. P1: Data Credibility & Logic Fixes (Priority: Medium)
*   **Clarify 10M Constraint**: In `chapter4_results_verification.md`, modify the text to "Target ≤ 10M, keeping representative baselines > 10M (e.g. UNO 28M) for comparison" to match the table content.
*   **Address UNetFormer Data**: Add a footnote or explanation for UNetFormer's `Rel-L2=0.9473` / `H_err=0.0000` to indicate this represents a trivial/overfitted solution under sparse observation, rather than a data error.
*   **Align EDSR Latency**: Update text in Chapter 4 to match the table value (`4.05 ms`), ensuring consistency.
*   **Refine Metric Definitions**: In 4.1.3, explicitly define PSNR's $I_{\max}$ and clarify that Rel-L2 and PSNR are calculated on the same scale/domain.
*   **Necessity Analysis**: Clarify the control group setup in 4.3.3 to better decouple SR and temporal stride effects.

#### 3. P2: Narrative & Formatting Polish (Priority: Low)
*   **Dataset Narrative**: In 4.1.1, explicitly state that "SWE is used for rapid pre-screening (Sec 4.2), while DRD/Darcy are used for main experiments".
*   **Symbol Unification**: Standardize $\mu, \sigma$ to $\mu_z, \sigma_z$ in Chapter 4 to match the Notation Table in Chapter 0.
*   **Citation Standardization**: Ensure citations in Chapters 1-2 follow a consistent format (APA style) where possible.

I will perform these edits sequentially across the files `chapter2_problem_framework.md`, `chapter4_results_verification.md`, and `chapter5_discussion_conclusion.md`.