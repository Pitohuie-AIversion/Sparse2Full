# 任务计划：整合与完善 3阶段与 E2E 训练策略的统计分析

## 1. 现状分析 (Current State Analysis)

经过对 `thesis_paper/manuscript_5_chapter/chapter4_results_verification.md` 的检查，**您的论文中确实已经包含了 3阶段（Stage 3）和 E2E 的统计数据，但它们目前是分散且不完整的**：

1. **数据分散在两个表格中**：
   - **表 4-8** 对比了 **Stage 2 (Freeze Spatial)** 和 **Stage 3 (Unfreeze All / 联合微调)**。数据显示 Stage 3 的 Rel-L2 为 0.2030，fRMSE-High 为 2.46。
   - **表 4-9** 对比了 **Two-Stage (即 Stage 2)** 和 **End-to-End (E2E)**。数据显示 E2E 的 Rel-L2 为 0.1783，fRMSE-High 为 0.3340。
2. **缺乏直观的全局对比**：读者无法在一个表格里直观看到 “Stage 2 vs Stage 3 vs E2E” 的全景对比。例如，E2E 的 fRMSE-High (0.3340) 实际上远好于 Stage 3 (2.46)，但因为分在两个表里，这种绝对优势没有被清晰地凸显出来。
3. **指标维度略有缺失**：虽然列出了训练耗时，但在统一对比时，可以把 3阶段 的总耗时（Stage 1 + Stage 2 + Stage 3 = 37.7h + 12.5h = 50.2h）也明确列出，与 E2E (88.3h) 进行对比。

## 2. 拟定修改方案 (Proposed Changes)

为了让论文的论证更加严密和直观，建议将 4.3.2 节（表4-8）和 4.6.1 节（表4-9）的相关内容进行**深度重构与合并**，具体步骤如下：

### 步骤 1：合并表格，构建全景对比表
创建一个全新的综合表格，将所有训练策略并列：
- **Two-Stage (Stage 1+2)**：作为稳健的基线。
- **Three-Stage (Stage 1+2+3)**：展示联合微调的效果（高频改善但整体误差上升的 trade-off）。
- **End-to-End (E2E)**：展示直接联合优化的最终性能天花板。

**拟合并的表格结构示例**：
| 训练策略 (Strategy) | 阶段构成 | Rel-L2 $\downarrow$ | PSNR (dB) $\uparrow$ | SSIM $\uparrow$ | fRMSE-High $\downarrow$ | 总训练耗时 (h) | 核心特性 |
| :--- | :--- | :---: | :---: | :---: | :---: | :---: | :--- |
| **Two-Stage** | Stage 1 + 2 | 0.1787 | 31.20 | 0.8837 | 4.4524 | 37.7 | 收敛极稳，耗时短 |
| **Three-Stage** | Stage 1 + 2 + 3 | 0.2030 | 29.85 | 0.8869 | 2.4600 | 50.2 | 高频细节增强，但不稳定 |
| **End-to-End** | 直接 E2E | **0.1783** | **31.15** | **0.8860** | **0.3340** | 88.3 | **综合性能最佳，高频极优** |

### 步骤 2：重构 4.6 节的叙述逻辑
在 4.6 节集中讨论训练策略，叙述逻辑调整为：
1. **先讲 Two-Stage**：作为工程上最快、最稳的 Baseline。
2. **再讲 Three-Stage 的局限性**：尝试在 Stage 2 后解冻全参（Stage 3），虽然高频指标（fRMSE-High）改善了 44%，但全场误差（Rel-L2）反而上升（0.1787 -> 0.2030），说明“先冻结后解冻”的策略容易陷入局部极小值或引发遗忘。
3. **最后引出 E2E 的优越性**：E2E 克服了上述问题，不仅 Rel-L2 最低（0.1783），且高频误差极低（0.3340），证明了从头开始联合优化空间与时序梯度的必要性。最后客观指出 E2E 的代价是训练时间增加了约 1.3 倍（37.7h -> 88.3h）。

### 步骤 3：清理冗余信息
- 移除原 4.3.2 节中孤立的 表 4-8，将其核心结论转移至 4.6 节。避免文中出现重复的比较。

## 3. 假设与决策 (Assumptions & Decisions)
- **决策**：将分散的论证整合到 4.6 节“分阶段顺序训练与端到端联合优化分析”中，这会使文章结构更紧凑。
- **假设**：表格中的数据（如 Stage 3 的 Rel-L2 为 0.2030）是准确且最终的。我们将直接使用当前 Markdown 文件中记载的数值。

## 4. 验证步骤 (Verification)
- 检查合并后的表格在 Markdown 渲染下格式是否正确。
- 确认上下文的交叉引用（如“表 4-8”、“表 4-9”）在重新编号后是否逻辑一致。
- 确认没有遗漏之前表格中的重要结论。